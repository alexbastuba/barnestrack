/**
 * `barnestrack v<major>.<minor>.<patch> (<git-sha7>)`, computed at build time
 * from package.json and the current commit (see vite.config.ts). D12.
 */
export type ToolVersion = string;

export const TOOL_NAME = 'barnestrack';
