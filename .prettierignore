dist
node_modules
coverage
notes

# Generated by scripts/gen-pts-fixture.sh from ffprobe output, one line per
# file. The tests JSON.parse them, so reformatting would not break a test — it
# would explode each one into thousands of lines of diff noise. They are
# regenerated by the script, never edited.
tests/fixtures/*.pts.json

# Hand-aligned measurement tables. Prettier's markdown table formatter rewrites
# the column widths, which makes every later measurement a whole-file diff.
prototypes/**/RESULTS.md
