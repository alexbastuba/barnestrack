dist
node_modules
coverage
notes

# Generated by scripts/gen-pts-fixture.sh from ffprobe output: the fixtures are
# compared byte for byte against what that script writes, so reformatting them
# would make the sample-table tests fail against their own source of truth.
tests/fixtures/*.pts.json

# Hand-aligned measurement tables. Prettier's markdown table formatter rewrites
# the column widths, which makes every later measurement a whole-file diff.
prototypes/**/RESULTS.md
